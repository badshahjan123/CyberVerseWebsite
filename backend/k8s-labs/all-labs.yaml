apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyber-lab-forensics
  labels:
    app: cyber-lab
    lab-id: linux-forensics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyber-lab
      lab-id: linux-forensics
  template:
    metadata:
      labels:
        app: cyber-lab
        lab-id: linux-forensics
    spec:
      containers:
        - name: lab-container
          image: badshahjan/lab1:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /home/labuser/evidence
              echo "The password is hidden in the binary" > /home/labuser/evidence/.secret_note
              echo "U0VDUkVUX0tFWV8xMjM0NQ==" | base64 -d > /home/labuser/evidence/mystery.bin
              echo 'echo "FLAG" > /tmp/flag_storage' >> /home/labuser/.bash_history
              echo "FLAG{FORENSIC_DISCOVERY_COMPLETE}" > /home/labuser/.backup_flag.txt
              chown -R 1000:1000 /home/labuser
              ttyd -W -p 80 -i 0.0.0.0 bash
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: cyber-lab-forensics-service
  labels:
    lab-id: linux-forensics
spec:
  selector:
    app: cyber-lab
    lab-id: linux-forensics
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 80
      nodePort: 32083
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyber-lab-malware
  labels:
    app: cyberverse-lab
    lab-id: malware
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyberverse-lab
      lab-id: malware
  template:
    metadata:
      labels:
        app: cyberverse-lab
        lab-id: malware
    spec:
      containers:
        - name: malware-container
          image: badshahjan/malware-lab:latest
          env:
            - name: DECRYPTION_KEY
              value: "MALWARE_SECRET_ACCESS_KEY"
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /etc/malware
              echo '{"author": "badshahjan", "version": "2.4.1", "target": "finance_db"}' > /etc/malware/config.json
              echo "Server listening on port 4444" > /var/log/network_trace.log
              echo "The final flag is hidden in /var/tmp/.hidden_flag" >> /root/.bash_history
              mkdir -p /var/tmp
              echo "flag{CYBERVERSE_MALWARE_ELITE_2024}" > /var/tmp/.hidden_flag
              ttyd -W -p 7681 sh
          imagePullPolicy: Always
          ports:
            - containerPort: 7681
---
apiVersion: v1
kind: Service
metadata:
  name: cyber-lab-malware-service
  labels:
    lab-id: malware
spec:
  selector:
    app: cyberverse-lab
    lab-id: malware
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 7681
      nodePort: 32230
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyber-lab-web-security
  labels:
    app: cyberverse-lab
    lab-id: web-security
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyberverse-lab
      lab-id: web-security
  template:
    metadata:
      labels:
        app: cyberverse-lab
        lab-id: web-security
    spec:
      containers:
        - name: web-security-container
          image: badshahjan/web-vuln-advanced:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 7681
---
apiVersion: v1
kind: Service
metadata:
  name: cyber-lab-web-security-service
  labels:
    lab-id: web-security
spec:
  selector:
    app: cyberverse-lab
    lab-id: web-security
  ports:
    - protocol: TCP
      port: 8087
      targetPort: 7681
      nodePort: 32235
  type: NodePort

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyber-lab-active-directory
  labels:
    app: cyber-lab
    lab-id: active-directory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyber-lab
      lab-id: active-directory
  template:
    metadata:
      labels:
        app: cyber-lab
        lab-id: active-directory
    spec:
      containers:
        - name: lab-container
          image: badshahjan/lab1:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /home/labuser/ad-data

              echo "cyberverse.local" > /home/labuser/ad-data/domain.txt

              printf "Administrator\nJohn\nSarah\nMike\nHelpDesk\n" > /home/labuser/ad-data/users.txt

              printf "Domain Admins\nIT Support\nHelp Desk\n" > /home/labuser/ad-data/groups.txt

              echo "DC01" > /home/labuser/ad-data/domain-controller.txt

              chown -R 1000:1000 /home/labuser

              ttyd -W -p 80 -i 0.0.0.0 bash

          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: cyber-lab-active-directory-service
spec:
  selector:
    app: cyber-lab
    lab-id: active-directory
  ports:
    - protocol: TCP
      port: 8088
      targetPort: 80
      nodePort: 32240
  type: NodePort

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyber-lab-container-breakout
  labels:
    app: cyber-lab
    lab-id: container-breakout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyber-lab
      lab-id: container-breakout
  template:
    metadata:
      labels:
        app: cyber-lab
        lab-id: container-breakout
    spec:
      containers:
        - name: lab-container
          image: badshahjan/lab1:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /home/labuser/docker-recon
              touch /home/labuser/.dockerenv
              echo "FLAG{DOCKER_ENV_DETECTED}" > /home/labuser/docker-recon/recon_flag.txt
              
              mkdir -p /mnt/host_fs
              echo "FLAG{CAP_SYS_ADMIN_EXPLOITED}" > /mnt/host_fs/host_flag.txt
              
              mkdir -p /host_etc
              echo "FLAG{SHADOW_FILE_READ}" > /host_etc/shadow_flag.txt
              
              echo "FLAG{FULL_CONTAINER_ESCAPE}" > /root/master_escape_flag.txt

              mkdir -p /var/run
              echo "FLAG{DOCKER_SOCK_ABUSED}" > /var/run/docker.sock

              chown -R 1000:1000 /home/labuser

              ttyd -W -p 80 -i 0.0.0.0 bash
          env:
            - name: AWS_SECRET_ACCESS_KEY
              value: "FLAG{ENV_VAR_SECRET_LEAKED}"
          ports:
            - containerPort: 80
          securityContext:
            privileged: true

---
apiVersion: v1
kind: Service
metadata:
  name: cyber-lab-container-breakout-service
spec:
  selector:
    app: cyber-lab
    lab-id: container-breakout
  ports:
    - protocol: TCP
      port: 8089
      targetPort: 80
      nodePort: 32245
  type: NodePort
